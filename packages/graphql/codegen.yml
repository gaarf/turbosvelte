overwrite: true
schema: https://graphqlpokemon.favware.tech/
documents: ../*/src/**/*.graphql
generates:
  build/index.ts:
    hooks:
      afterOneFileWrite:
        - node rewrite.js
    plugins:
      - typescript
      - typescript-operations
      - typed-document-node
